#include<iostream> 
using namespace std;
int main() {
   int a = 10, b = 5;
   int sum = a + b;

   cout << "sum = " << (a+b) << endl;
   cout << "diff = "<< (a-b) << endl;
   cout << "pro = "<< (a*b) << endl;
   cout << "div = "<< (a/b) << endl; //2
   cout << "mod = "<< (a%b) << endl; // 0

   
    return 0;

}